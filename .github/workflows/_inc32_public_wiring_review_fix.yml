name: Increment 32 API Manifest Diagnostic

on:
  push:
    branches:
      - automation/inc32-public-wiring-review-fix

permissions:
  contents: read
  actions: read

jobs:
  collect:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Download and inspect failing Increment 14 job log
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          curl --fail --location \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "Accept: application/vnd.github+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/pysolvesemi/Nodal/actions/jobs/99315063505/logs \
            --output /tmp/increment14-job-logs.zip
          mkdir -p /tmp/increment14-job-logs
          unzip -q /tmp/increment14-job-logs.zip -d /tmp/increment14-job-logs
          grep -RIn -B 100 -A 160 \
            -E 'check_api_candidate|Expected frozen API manifest|current candidate sources' \
            /tmp/increment14-job-logs \
            > /tmp/increment14-api-manifest-context.log
          test -s /tmp/increment14-api-manifest-context.log

      - name: Upload diagnostic context
        uses: actions/upload-artifact@v4
        with:
          name: increment14-api-manifest-context
          path: /tmp/increment14-api-manifest-context.log
          if-no-files-found: error
          retention-days: 1
